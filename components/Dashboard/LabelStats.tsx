import React from "react";

type Props = {
  label: string;
  className?: string;
};
export default function LabelStats({ label, className }: Props) {
  // TODO: what does it want to show?
  return null;
}
